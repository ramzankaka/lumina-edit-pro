{
  "name": "lumina-edit-pro",
  "version": "1.0.0",
  "description": "Professional 100% Offline Photo & Passport Size Editor",
  "main": "main-electron.js",
  "scripts": {
    "start": "electron .",
    "package-win": "electron-packager . LuminaEditPro --platform=win32 --arch=x64 --out=dist --overwrite"
  },
  "dependencies": {},
  "devDependencies": {
    "electron": "^31.3.0",
    "electron-packager": "^17.1.2"
  }
}
